# openapi-typescript@7 declares peer typescript@^5.x, но монорепа на TypeScript 6.
# Это dev-only инструмент кодогенерации и с TS 6 работает корректно, поэтому
# принимаем несоответствие peer, чтобы `npm ci` (в т.ч. в CI drift-gate) проходил.
legacy-peer-deps=true
